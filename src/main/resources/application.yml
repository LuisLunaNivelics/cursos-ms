server:
  port: 8083
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/curso
    username: root
    password: 12345
    driver-class-name: com.mysql.jdbc.Driver

